coverage:
  notify:
    slack:
      default: off
      fintruth-sdk:
        url: https://hooks.slack.com/services/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
        threshold: 0.01%
        flags:
          - auth
          - client
          - server
          - shared
          - validation
  status:
    project:
      default: off
      auth:
        flags: auth
      client:
        flags: client
      server:
        flags: server
      shared:
        flags: shared
      validation:
        flags: validation
flags:
  auth:
    paths:
      - packages/auth
  client:
    paths:
      - packages/client
  server:
    paths:
      - packages/server
  shared:
    paths:
      - packages/shared
  validation:
    paths:
      - packages/validation
