{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "~10.12.18",
    "del-cli": "~1.1.0",
    "lerna": "~3.9.1",
    "typescript": "~3.2.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna exec --parallel -- del-cli build",
    "prebuild": "yarn clean",
    "build": "lerna run build"
  }
}
